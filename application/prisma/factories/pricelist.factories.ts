// creates an entire price list with a product, and imported products

import { createSampleSession } from './session.factories';

export const createSamplePriceListWithProducts = async (overrides = {}) => {
  const supplier = await createSampleSession();
};
