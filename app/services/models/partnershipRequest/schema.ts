// TODO: add validation for partnership request models
// import { object } from "yup";
// priceListIds, recipientId, senderId, message, type, status
// const createOrUpdatePartnershipRequestSchema = object({

// })
